/* eslint-disable no-undef */
const APP_ENV = {
  TEST: 'test',
  LOCAL: 'local',
  DEV: 'development',
  PROD: 'production',
};

const NameSpaces = {
  Cls: 'node-test',
};

const constants = {
  NameSpaces,
  APP_ENV,
};

export { constants };
